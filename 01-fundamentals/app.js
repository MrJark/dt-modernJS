
let a = 10,
    b = 20,
    c = 30,
    d = 40;

console.log( {a, b, c} ); // los corchetes permiten mostrar el valor y la key a la vez
console.table({a,b,c});// da los valores en formato tabla pero son necesarios los corchetes
